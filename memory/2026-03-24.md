- Built a runnable Next.js ecommerce monorepo in `/root/.openclaw/workspace` with apps `admin`, `storefront-default`, and `storefront-minimal`, plus packages for `core`, `db`, `theme-sdk`, `config`, `ui`, and `integrations`.
- Implemented Prisma/Postgres commerce foundation with local Postgres installed and configured, `.env` using `postgresql://postgres:postgres@127.0.0.1:5432/culi_commerce?schema=public`, and working `pnpm db:generate`, `pnpm db:push`, `pnpm db:seed` flows.
- Seeded demo admin account `admin@culi.local` with password `dev-only` (bcrypt-hashed in DB); admin auth now uses signed cookies, DB-backed sessions, rotation, expiry, persistent rate limiting, CSRF protection, revoke-current/revoke-others/revoke-single session controls, and session audit APIs/pages.
- Commerce flow implemented and verified end-to-end via HTTP integration tests: add to cart, cart read, checkout, order creation, and admin order viewing.
- Inventory handling includes reservation records (`InventoryReservation`), lazy + cron cleanup (`scripts/run-reservation-cleanup.sh`, every 15 minutes via crontab), stock validation, and checkout idempotency via `Order.idempotencyKey`.
- Coupon system supports percentage/fixed coupons plus rules for minimum subtotal, minimum quantity, product slug, and category slug; admin coupon CRUD exists via UI/API.
- Added observability primitives: structured JSON logs, DB-backed metric counters (`MetricCounter`), and status/health/metrics APIs under admin.
- Added audit trail storage (`AuditLog`) and verified admin CRUD traceability for product/category/coupon create/update/delete via HTTP API tests; audit payloads are object-shaped and CRUD counts exist in DB.
- Test commands now include `pnpm test`, `pnpm test:e2e`, and `pnpm test:admin-crud`; verified suites passed during this session, including admin CRUD via HTTP APIs and session control APIs.
- Browser-level E2E was deferred because browser host status reported enabled but not running (`running: false` / no Chromium attached), so verification relied on real HTTP/API/DB integration instead.
- Vercel token was provided and stored locally only at `/root/.openclaw/workspace/.secrets/vercel.token`; `.secrets/` was added to `.gitignore` so the token is not tracked in repo files.
- Staging is now live on this VPS using self-signed TLS behind Nginx with domains:
  - `https://staging.45.77.32.128.sslip.io`
  - `https://admin-staging.45.77.32.128.sslip.io`
- Host live staging runtime is managed by systemd services:
  - `culi-storefront-staging`
  - `culi-admin-staging`
  with app code deployed at `/opt/culi-commerce` and reverse proxy active on ports 80/443 via Nginx.
- Verified live staging URLs on this host:
  - `https://admin-staging.45.77.32.128.sslip.io/api/system/health` returns `{ ok: true, service: "admin", db: "up", ... }`
  - `https://staging.45.77.32.128.sslip.io/` returns HTTP 200 through Nginx.
- Prisma staging deploy flow was hardened:
  - fixed `packages/db/prisma/migrations/migration_lock.toml` provider to `postgresql`
  - added `db:migrate:deploy`
  - added ENV-file-aware loader script `scripts/load-env.sh`
  - verified `migrate deploy` works on a fresh staging DB
  - added baseline path `scripts/prisma-baseline-existing-db.sh` for databases previously created via `db:push`.
- Staging deploy artifacts now include:
  - `deploy/install-staging-host.sh`
  - `deploy/enable-staging-host.sh`
  - `deploy/nginx/staging-tls.conf`
  - `deploy/systemd/culi-storefront-staging.service`
  - `deploy/systemd/culi-admin-staging.service`
  - `scripts/gen-staging-selfsigned-certs.sh`
  - `docs/deploy-staging.md`
  - `docs/deploy-staging-cutover.md`
- Quick staging ops scripts were added and verified:
  - `deploy/smoke-staging.sh`
  - `deploy/backup-staging.sh`
  - `deploy/rollback-staging.sh`
- Verified staging backup path currently creates backups under `/opt/culi-backups/<UTC-stamp>` and updates `/opt/culi-backups/latest`.
- Verified rollback from backup works end-to-end on this VPS after resetting/restoring the staging DB and rerunning smoke checks.

## 2026-03-24 Cu Li DM recovery handoff
- Chủ Nhân yêu cầu không được để Cu Li mất trí nhớ dự án gần hoàn tất.
- Session DM Telegram của Chủ Nhân trên Cu Li bị kẹt/fail do model errors trước đó (429 codex, rồi fallback claude thiếu credentials).
- Đã quyết định ưu tiên bảo toàn trí nhớ bằng file-backed context trước khi gỡ session.
- Trạng thái dự án cần giữ:
  - staging stable
  - release path sạch
  - operability/consistency lõi đã chốt
  - production deploy/smoke/env-check/cutover docs đã có
  - order lifecycle admin đã xong và polish guard/filter/quick actions đã xong
  - Cu Li vừa chuyển sang nhánh inventory / stock movement visibility cho admin và đang sửa code thật trong workspace
- Nếu phải reset session DM, chỉ reset session Telegram direct bị kẹt; không xoá workspace, memory, repo, docs, hay transcript backups.
